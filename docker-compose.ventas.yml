version: '3.8'

services:
  backend-ventas:
    image: ${DOCKER_USERNAME}/backend-ventas:latest
    container_name: p2_backend_ventas
    restart: on-failure
    ports:
      - "8082:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=mysql
      # Apunta dinámicamente a la IP Privada de la EC2 de Despachos inyectada por GitHub Actions
      - SPRING_DATASOURCE_URL=jdbc:mysql://${DB_HOST_IP}:3306/${DB_NAME:-p2_db}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=${DB_USER:-admin_p2}
      - SPRING_DATASOURCE_PASSWORD=${DB_PASSWORD:-super_secret_password}
    networks:
      - ventas_network

networks:
  ventas_network:
    driver: bridge