apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-ventas-deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-ventas
  template:
    metadata:
      labels:
        app: api-ventas
    spec:
      containers:
      - name: ventas
        image: 011281349473.dkr.ecr.us-east-1.amazonaws.com/ep3devops/backend-ventas:latest
        ports:
        - containerPort: 8082
        env:
        - name: SERVER_PORT
          value: "8082" # Fuerza a Spring Boot a usar este puerto
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:mysql://mysql-db:3306/proyecto_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true"
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom: { secretKeyRef: { name: db-secrets, key: DB_USERNAME } }
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom: { secretKeyRef: { name: db-secrets, key: DB_PASSWORD } }
        - name: DB_ENDPOINT
          value: "mysql-db"
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "250m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: api-ventas-service
spec:
  selector:
    app: api-ventas
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082
  type: LoadBalancer
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: api-ventas-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: api-ventas-deploy
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50